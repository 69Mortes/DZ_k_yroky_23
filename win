# Условие задачи:
# Написать функцию, которая заполняет массив случайными числами в диапазоне,
# указанном пользователем. Функция должна принимать два аргумента -
# начало диапазона и его конец, при этом ничего не возвращать.
# Вывод значений элементов массива должен происходить
# в основной ветке программы.

import random # Импортируем библиотеку рандом
# Функция:
def DZ_23(a, b): # Создаем функцию с 2 аргументами
    list_1 = [] # Создаем пустой список для хранения массива
    for N in range(L): # Создаем цикл для создания массива
        list_1.append(random.randint(a, b)) # Добавляем в наш массив случайное число
    return list_1 # Повторяем цикл для заполнения массива

# Основная ветка программы:
a = int(input("Введите начало диапазона: ")) # Указываем число, с которого начнется рандом
b = int(input("Введите конец диапазона: ")) # Указываем число, котором рандом закончится
L = int(input("Введите длину массива: ")) # Указываем число, отображающее количество чисел в массиве
list_1 = DZ_23(a, b) # Вызываем функцию заполнения массива случайными числами
print(list_1) # Выводим на экран наш массив  
